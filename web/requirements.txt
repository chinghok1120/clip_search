# CLIP Search Web Interface Dependencies

# Core dependencies (from parent project)
torch>=2.0.0
open-clip-torch>=2.20.0
transformers>=4.40.0  # Required for SigLIP HF tokenizer (not needed by EVA-02)
faiss-gpu>=1.7.4
pillow>=10.0.0
numpy>=1.24.0

# Web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Pydantic for API schemas
pydantic>=2.0.0

# Optional: LoRA support (for future)
# peft>=0.7.0

# Development
# pytest>=7.4.0
# httpx>=0.25.0  # For testing FastAPI
